/**
 * Shared pill/badge geometry. Pair with a `bg-* text-*` tone class.
 * For a ready-made status badge use `StatusBadge` from `admin-kit`.
 */
export const PILL_BASE =
  "inline-flex rounded-full px-2.5 py-1 text-xs font-semibold whitespace-nowrap";
